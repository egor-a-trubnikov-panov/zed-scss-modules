# SCSS Modules for Zed

Navigate from JavaScript and TypeScript CSS module references to class definitions
inside `*.module.scss` files.

## Requirements

Install `cssmodules-language-server` and make sure it is available in your `PATH`:

```sh
npm i -g cssmodules-language-server
```

## Supported Languages

- JavaScript
- JSX
- TypeScript
- TSX
